# Environment configuration for the Nimbus API server
NODE_ENV=development
HOST=127.0.0.1
PORT=4000
CORS_ORIGIN=http://localhost:3000
# Defaults to <repo>/workspaces/demo if unset — override with an absolute path to point at a different workspace.
WORKSPACE_ROOT=
